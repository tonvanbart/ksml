pipelines:
  main:
    from: test_input
    via:
    - type: mapValues
      mapper:
        code: 'return value[''type''] + '' '' + value[''value'']

          '
        resultType: string
    to: test_output
