pipelines:
  main:
    from: test_input
    via:
    - type: flatMap
      mapper:
        resultType: '[(string,string)]'
        code: 'return [ (key,value),(key + ''-b'',value + ''-b'') ]

          '
    to: test_output
