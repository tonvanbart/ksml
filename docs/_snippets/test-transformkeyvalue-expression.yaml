pipelines:
  main:
    from: test_input
    via:
    - type: transformKeyValue
      mapper:
        expression: (value['city'], value['type'] + ' ' + value['value'])
        resultType: (string,string)
    to: test_output
